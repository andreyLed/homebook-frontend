import ng from 'angular';
import InputOutputComponent from './inputOutput.component';

export default ng.module('inputOutput.components.app', [])
    .component('inputOutput', InputOutputComponent)
    .name;